#include <stdio.h>
#include <stdlib.h>

/*
* If you’re going to change a string, make a copy.
* The truth is that if you want to change the contents of a string, you’ll need * to work on a copy.
* If you create a copy of the string in an area of memory that’s not read-only,
* there won’t be a problem if you try to change the letters it contains.
* One way we can make a copy is to create the string as a new array:
*/

int main()
{
    // Make cards an array:
    char cards[] = "JQK";
    return 0;
}
